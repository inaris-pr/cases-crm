auto-install-peers=true
strict-peer-dependencies=false
prefer-workspace-packages=true
# Hoisted node_modules layout — friendlier on Windows and avoids the
# multi-version esbuild postinstall version-check failure.
node-linker=hoisted
